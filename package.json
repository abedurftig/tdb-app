{
  "scripts": {
    "set:env": "node setenv.js > .env",
    "dev": "npm run set:env && env-cmd .env poi",
    "build:prod": "NODE_ENV=production npm run set:env && env-cmd .env poi build",
    "heroku": "http-server public -p $PORT"
  },
  "engines": {
    "node": "8.1.3",
    "npm": "5.0.3",
    "yarn": "0.27.5"
  },
  "dependencies": {
    "http-server": "^0.10.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "devDependencies": {
    "babel-plugin-react-require": "^3.0.0",
    "babel-preset-react-app": "^3.1.0",
    "cross-env": "^5.1.1",
    "env-cmd": "^7.0.0",
    "node-sass": "^4.7.2",
    "poi": "^9.6.1",
    "poi-preset-react": "^9.2.1",
    "sass-loader": "^6.0.6"
  }
}
